#!/usr/bin/env bash
set -eux

git fetch --all

COMMON_COMMIT=$(git merge-base HEAD origin/master)

if [ -z $(git diff HEAD..$COMMON_COMMIT app/assets) ]; then
  exit 0
else
  git show --pretty="" --name-only HEAD | grep public/assets
fi
