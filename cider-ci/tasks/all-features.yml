---
tasks:
- name: login/login
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/login/login.feature"
- name: manage/acknowledge/add_item
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/acknowledge/add_item.feature"
- name: manage/acknowledge/delete_lines
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/acknowledge/delete_lines.feature"
- name: manage/acknowledge/edit_order_line
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/acknowledge/edit_order_line.feature"
- name: manage/acknowledge/edit_purpose
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/acknowledge/edit_purpose.feature"
- name: manage/acknowledge/quick_approve
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/acknowledge/quick_approve.feature"
- name: manage/acknowledge/reject
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/acknowledge/reject.feature"
- name: manage/add_item
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/add_item.feature"
- name: manage/availability/allocating
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/availability/allocating.feature"
- name: manage/availability/availability
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/availability/availability.feature"
- name: manage/booking_calendar/availability_changes
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/booking_calendar/availability_changes.feature"
- name: manage/booking_calendar/closed_day_alert
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/booking_calendar/closed_day_alert.feature"
- name: manage/booking_calendar/summary_of_lines_in_calendar
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/booking_calendar/summary_of_lines_in_calendar.feature"
- name: manage/calendar
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/calendar.feature"
- name: manage/change_language
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/change_language.feature"
- name: manage/contract/contracts
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/contract/contracts.feature"
- name: manage/daily_view/daily_view
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/daily_view/daily_view.feature"
- name: manage/delegation
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/delegation.feature"
- name: manage/displaying_problems
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/displaying_problems.feature"
- name: manage/explorative_search
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/explorative_search.feature"
- name: manage/hand_over/add
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/hand_over/add.feature"
- name: manage/hand_over/assign
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/hand_over/assign.feature"
- name: manage/hand_over/calendar
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/hand_over/calendar.feature"
- name: manage/hand_over/change_borrower
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/hand_over/change_borrower.feature"
- name: manage/hand_over/delete
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/hand_over/delete.feature"
- name: manage/hand_over/edit_reservation
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/hand_over/edit_reservation.feature"
- name: manage/hand_over/hand_over
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/hand_over/hand_over.feature"
- name: manage/hand_over/sign_contract
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/hand_over/sign_contract.feature"
- name: manage/inventory/categories
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/categories.feature"
- name: manage/inventory/copy_item
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/copy_item.feature"
- name: manage/inventory/create_item
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/create_item.feature"
- name: manage/inventory/create_model_with_packages
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/create_model_with_packages.feature"
- name: manage/inventory/edit_item
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/edit_item.feature"
- name: manage/inventory/inventory
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/inventory.feature"
- name: manage/inventory/inventory_csv_export
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/inventory_csv_export.feature"
- name: manage/inventory/inventory_expert_search
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/inventory_expert_search.feature"
- name: manage/inventory/inventory_helper
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/inventory_helper.feature"
- name: manage/inventory/model
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/model.feature"
- name: manage/inventory/model_properties
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/model_properties.feature"
- name: manage/inventory/option
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/option.feature"
- name: manage/inventory/retire_item
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory/retire_item.feature"
- name: manage/inventory_pool/groups
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory_pool/groups.feature"
- name: manage/inventory_pool/inventory_pool_basic_information
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory_pool/inventory_pool_basic_information.feature"
- name: manage/inventory_pool/inventory_pool_settings
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/inventory_pool/inventory_pool_settings.feature"
- name: manage/lending/day_navigation
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/lending/day_navigation.feature"
- name: manage/lending/lending
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/lending/lending.feature"
- name: manage/lending/tabs_navigation
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/lending/tabs_navigation.feature"
- name: manage/linegroup/selection
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/linegroup/selection.feature"
- name: manage/mails/delegation_mail
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/mails/delegation_mail.feature"
- name: manage/mails/mail_templates
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/mails/mail_templates.feature"
- name: manage/manage_templates
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/manage_templates.feature"
- name: manage/manage_users
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/manage_users.feature"
- name: manage/navigation
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/navigation.feature"
- name: manage/not_logged_in_information
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/not_logged_in_information.feature"
- name: manage/old_timeline
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/old_timeline.feature"
- name: manage/order/change_orderer
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/order/change_orderer.feature"
- name: manage/order/contract
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/order/contract.feature"
- name: manage/order/manage_orders
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/order/manage_orders.feature"
- name: manage/picking_list
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/picking_list.feature"
- name: manage/purpose
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/purpose.feature"
- name: manage/search
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/search.feature"
- name: manage/software/copy_license
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/software/copy_license.feature"
- name: manage/software/create_software
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/software/create_software.feature"
- name: manage/software/delete_software
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/software/delete_software.feature"
- name: manage/software/edit_software
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/software/edit_software.feature"
- name: manage/software/search_for_software
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/software/search_for_software.feature"
- name: manage/software/software_create
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/software/software_create.feature"
- name: manage/take_back/close_contract
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/take_back/close_contract.feature"
- name: manage/take_back/edit_take_back_end_date
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/take_back/edit_take_back_end_date.feature"
- name: manage/take_back/take_back
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/take_back/take_back.feature"
- name: manage/timeline
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/timeline.feature"
- name: manage/users
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/users.feature"
- name: manage/value_list
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/value_list.feature"
- name: manage/verification
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/verification.feature"
- name: manage/visits
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/manage/visits.feature"
- name: technical/automatic_email_notification
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/automatic_email_notification.feature"
- name: technical/availability_inventory_pool_story
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/availability_inventory_pool_story.feature"
- name: technical/availability_story
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/availability_story.feature"
- name: technical/categories
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/categories.feature"
- name: technical/field/field_instance_methods
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/field/field_instance_methods.feature"
- name: technical/field/provide_accessible_fields_model
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/field/provide_accessible_fields_model.feature"
- name: technical/groups
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/groups.feature"
- name: technical/inventory_pool/hand_over_visits_model
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/inventory_pool/hand_over_visits_model.feature"
- name: technical/inventory_pool/take_back_visits_model
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/inventory_pool/take_back_visits_model.feature"
- name: technical/inventory_story
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/inventory_story.feature"
- name: technical/item/search_model
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/item/search_model.feature"
- name: technical/performance_stability
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/performance_stability.feature"
- name: technical/user/suspend
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/user/suspend.feature"
- name: technical/visit/hand_over_and_take_back_model
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/visit/hand_over_and_take_back_model.feature"
- name: technical/visit/overdue_model
  scripts:
    test:
      body: |-
        #!/usr/bin/env bash
        set -euo pipefail
        mkdir -p log
        xvfb-run -a -e log/xvfb.log ./bin/cucumber -p default --strict "features/technical/visit/overdue_model.feature"
