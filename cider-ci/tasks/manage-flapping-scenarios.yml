---
tasks:
- name: features/manage/acknowledge/add_item.feature:41
  scripts:
    test:
      body: |-
        set -eux
        mkdir -p log
        bundle exec cucumber --strict features/manage/acknowledge/add_item.feature:41
- name: features/manage/calendar.feature:12
  scripts:
    test:
      body: |-
        set -eux
        mkdir -p log
        bundle exec cucumber --strict features/manage/calendar.feature:12
- name: features/manage/inventory/inventory.feature:219
  scripts:
    test:
      body: |-
        set -eux
        mkdir -p log
        bundle exec cucumber --strict features/manage/inventory/inventory.feature:219
- name: features/manage/not_logged_in_information.feature:9
  scripts:
    test:
      body: |-
        set -eux
        mkdir -p log
        bundle exec cucumber --strict features/manage/not_logged_in_information.feature:9
- name: features/manage/take_back/edit_take_back_end_date.feature:17
  scripts:
    test:
      body: |-
        set -eux
        mkdir -p log
        bundle exec cucumber --strict features/manage/take_back/edit_take_back_end_date.feature:17
- name: features/manage/value_list.feature:30
  scripts:
    test:
      body: |-
        set -eux
        mkdir -p log
        bundle exec cucumber --strict features/manage/value_list.feature:30
- name: No Tasks for spec/features/manage
  scripts:
    test:
      body: echo OK
