.row

  - @model.attachments.each do |attachment|
    %a.line.start-even.blue{:href => get_attachment_path(attachment.id), :target => "_blank"}
      .line-col
        %i.fa.fa-file-text.margin-right-s
        = attachment.filename
