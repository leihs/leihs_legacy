.customer
  %label= _("Borrower")
  %span.name= @user
  - contact_person = ( @delegated_user or @user )
  - if @delegated_user
    %br
    %span.name= contact_person
  - if contact_person.address
    %span.street= contact_person.address
  %span.zip_city= "%s %s" % [contact_person.zip, contact_person.city]
