:ruby
  diff_to_today = Integer(@date - Date.today).presence || 0

%h2.headline-m.light
  - if diff_to_today == 0
    = _("Today")
  - else
    = diff_to_today.abs
    = n_('day', 'days', diff_to_today.abs)

    - if diff_to_today > 0
      = _('in the future')
    - else
      = _('in the past')
