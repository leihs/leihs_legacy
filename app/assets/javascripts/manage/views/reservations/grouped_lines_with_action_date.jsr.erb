<h3 class='headline-s padding-inset-s'>
  {{if ~moment(date).endOf("day").diff(~moment().endOf("day"), "days") == 0}}
  <strong>{{jed "Today"/}}</strong>
  {{else}}
  {{day date/}}
  {{date date/}}
  {{/if}}
</h3>
{{partial "manage/views/reservations/grouped_lines" groups #view.ctx/}}
