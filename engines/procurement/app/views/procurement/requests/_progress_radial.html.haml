:ruby
  max = Integer(max.presence || 0)
  used = Integer(used.presence || 0)
  percentage = if max > 0
                 used * 100 / max
               elsif used > 0
                 100
               else
                 0
               end

.progress-radial{class: "progress-#{(percentage / 5).to_i * 5}", title: _('% used (category limit - total of selected requests)'), data: {toggle: 'tooltip'}}
  .overlay
    = succeed '%' do
      = percentage
